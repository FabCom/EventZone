<div class="d-flex justify-content-center mt-5" >
  <div class="d-flex justify-content-center mt-5 border border-primary rounded  p-3" style="">
      <%= simple_form_for(@event) do |f| %>
      <%= f.error_notification %>
      <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

      <div class="form-inputs">
        <%= f.input :title, label: "Titre" %>
        <%= f.input :start_date , label: "Date" %>
        <%= f.input :duration , label: "Durée" %>
        <%= f.input :description , label: "Description" %>
        <%= f.input :location , label: "Ville" %>
        <%= f.input :price , label: "Prix" %>
      </div>
        <%= f.hidden_field :administrator_id, value: if current_user then current_user.id end %>
      <div class="form-actions d-flex justify-content-center">
         <%= f.button :submit , value: "Valider", class: 'btn btn-primary' %>
      </div>
      <% end %>
  </div>
</div>



