<div class="d-flex justify-content-center">
  <h1 class="display-3">LES ÉVÉNEMENTS DU MOIS À VENIR...</h1>
  <%= if user_signed_in? then link_to 'CRÉER', new_event_path end%>
</div>

<%= render "events/list" %>